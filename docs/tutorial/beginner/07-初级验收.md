# 07 · 初级验收

← [06 表单与校验](./06-表单与校验.md) · 下一章 → [08 启用 SQLite](../intermediate/08-启用SQLite.md)

## 目标

确认初级目标全部达成，再进入中级（数据库）。

## 必做：逐项打勾

在你的练习应用上现场演示（给自己或同伴）：

1. [ ] `px --help` 可用；应用用 `px new` 创建  
2. [ ] `px dev` 能打开首页  
3. [ ] 能指出 `views/generated/` 且从未手改  
4. [ ] Home（或主页）Props/文案是你改过的  
5. [ ] 至少一条**命名**自定义路由（如 `/about`）可用  
6. [ ] 至少一个 JSON action：成功路径 + 422 路径  
7. [ ] CSRF 仍然开启  

## 口头题（不看文档答）

1. 为什么契约不能写在 TypeScript 里当唯一来源？  
2. `px dev` 起了哪两类进程？  
3. 下一阶段为什么要先启用 database feature，而不是直接写 SQL 文件？

参考答案要点：1）Rust 是唯一来源，TS 为生成物；2）Rust 应用 + Vite；3）脚手架/`phoenix-manage`/迁移注册依赖 feature 与生成文件。

## 未通过时

回到对应章节重做，**不要**开始第 08 章。

## 通过后

进入中级：[08 启用 SQLite](../intermediate/08-启用SQLite.md)。  
可选：给练习应用打 git tag `tutorial-beginner-done`。
